import React from "react";
import PropTypes from "prop-types";

export class RPGBattleCharacterGameComponent extends React.Component {
    // to manage battle character
    // load graphics? connect with a [[ObjectLoaderMesh]] ?
    // commands?
}

RPGBattleCharacterGameComponent.propTypes = {
    type:PropTypes.string.isRequired,
};
